/**
 * The one deployment-specific URL. Change this value when the site's domain changes.
 * Do not duplicate the origin elsewhere in application or build code.
 */
export const BASE_URL = 'https://heiket.com';
